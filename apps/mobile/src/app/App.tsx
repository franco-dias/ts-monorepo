import { SafeAreaView, View } from 'react-native';
import { Test } from '@workoutbuddy/native-core-ui';

export const App = () => {
  return (
    <SafeAreaView>
      <View>
        <Test />
      </View>
    </SafeAreaView>
  );
};

export default App;
